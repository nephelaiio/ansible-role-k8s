---
- name: converge

  hosts: localhost

  gather_facts: false

  roles:

    - nephelaiio.plugins

  tasks:

    - name: verify cluster
      include_role:
        name: nephelaiio.k8s
      vars:
        k8s_deploy: true
        k8s_verify: false
